{% extends 'base.html.twig' %}

{% block body %}
    <h1>All users</h1>

    <ul>
        {% for user in users %}
            <li>{{ user.id }} - {{ user.pseudo }}</li>
        {% endfor %}
    </ul>
{% endblock %}
